export class Player {
  position;
  #level;

  constructor(position, level) {
    this.position = position;
    this.#level = level;
  }

  move(direction) {
    this.position // pozycja tego gracza
    this.#level // poziom, na którym znajduje się ten gracz

    // jeśli możliwe rusza graczem, przesuwa skrzynki
  }
}
